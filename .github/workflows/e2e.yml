name: E2E

on:
  push:
    branches: [staging, main, master]
  pull_request:
    branches: [staging, main, master, 'release/**', 'hotfix/**']

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  e2e:
    name: Playwright
    uses: DaVinciBot/shared-workflows/.github/workflows/e2e.yml@v2.2.0
    secrets: inherit
    with:
      pnpm_version: 11.5.2
      node_version_file: .nvmrc
